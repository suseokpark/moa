# FAVMOA 사용성 개선 작업 큐

기준: [0.1.28 합성 페르소나 감사](PERSONA-AUDIT-2026-09-24.md). 실제 사용자 테스트가 아닌 합성 시나리오와 별도 예시 화면에서 나온 개선 후보다. 2026-09-26 09:00 KST까지 우선순위대로 진행하며, 그 시각 이후 새 변경은 시작하지 않는다. 후보를 다 처리하면 불필요한 변경을 추가하지 않는다.

**2026-09-25 / 0.1.41: 등록된 우선순위 1~7의 구현·검증을 마쳤다.** 아래 완료 기준과 버전별 QA가 범위다. 미완료 등록 후보는 없으며 전달 확인 후 예약을 중지한다. 여러 단계 Undo·개별 후보 검토의 페이지네이션·실제 설치 Chrome의 전체 통합 검증은 별도 제한/후속 판단이며 이번 완료에 포함하지 않는다.

## 전달 규칙

- AGENTS.md를 따르고 기존 사용자 변경을 보존한다. 완료한 작은 단위마다 회귀 테스트 → 전체 테스트 → 패키지 검사 → 관련 파일 커밋 → 원격 푸시 → 원격 HEAD 확인을 수행한다.
- 릴리스마다 patch 버전을 올리고 ZIP을 생성·검증한다. 사용자 데이터, 서명 키, 생성된 로컬 산출물은 공개 저장소에 올리지 않는다.
- 데이터는 합성 fixture/별도 localhost 예시만 사용한다. 브라우저 설치 확인과 실제 확장 데이터 동작 검증을 혼동하지 않는다.
- OAuth, 권한 추가, 클라우드 동기화, Notion 내부 UI, 웹스토어 게시는 범위 밖이다.
- 0.1.28 원본 감사 기록은 보존한다. 수정된 계약의 회귀 결과는 새 경로·버전으로 기록한다. 300/300 계약 통과를 실제 사용자 성공률로 부르지 않는다.
- 2026-09-25 Chrome 확장 관리 URL의 자동 접근이 브라우저 보안 정책으로 차단됐다. 다른 표면/CDP로 우회하지 않는다. 패키지·기존 설치 폴더 업데이트와 실제 Chrome 새로고침 확인을 구분하며, 새로고침은 사용자에게 안내한다.

## 우선순위와 완료 기준

| 순서 | 후보 | 상태 | 완료 기준 |
| --- | --- | --- | --- |
| 1 | 접기·펼치기의 편집 Undo 보호 | 0.1.29 구현·검증 완료 | 삭제/추가 후 명시적 폴딩 → Undo가 내용 편집을 취소하고 선택한 폴딩 유지. 빈/손상/충돌 이력 부활 없음. |
| 2 | 충돌 후 명시적 최신 목록 검토 | 0.1.30~0.1.36 지정 대화상자 구현·검증 완료 | 선택·입력 보존, 검토는 읽기만, 확인 후 별도 저장, 재충돌 시 덮어쓰기 없음. |
| 3 | 수동 추가·이동 결과 표시 | 0.1.37 구현·검증 완료 | 검색 해제, 목적지와 상위 경로 펼침, 결과에 초점. 실패/취소/no-op 시 맥락 유지. |
| 4 | 보관함 전환 시 검색 정리 | 0.1.38 구현·검증 완료 | 다른 유효 보관함 선택 시 검색 초기화, 같은/무효 선택 보존, 쓰기·Undo 없음, 늦은 결과 보호. |
| 5 | 순서 독립 다중 키워드 검색 | 0.1.39 메인 트리 구현·검증 완료 | 공백 단위 AND·제목/URL/조상 경로·순서 독립, 분기 격리·경로·선택 보존. 후보 창은 기존 계약. |
| 6 | 대량 후보 선택 반복 줄이기 | 0.1.40 구현·검증 완료 | 표시 범위와 결과 전체 선택 분리, 1,000개 합집합 제한·숨은 선택·취소·원자적 Undo 유지. 개별 검토 더 보기는 유지. |
| 7 | 가져오기 중 새 대상 그룹 | 0.1.41 구현·검증 완료 | 선택·검색·이름 유지, 명시적 위치 선택, 그룹+링크 단일 저장/Undo, 취소·충돌 시 데이터 보존. |

전체 다단계 Undo나 휴지통은 별도 설계 후보이며 마지막 내용 편집 1회 정책을 유지한다. 5~7은 합성 재현·경계 시험으로 구현 범위를 정해 완료했으며 실제 사용자 이해도나 만족도를 검증했다는 뜻은 아니다.

## 다음 실행에 필요한 설계 메모

### 2. 충돌 복구

**공용 가져오기 창은 0.1.30에서 완료됐다. 반복 구현하지 않는다.** `sidepanel.js`의 `requireResult`는 충돌의 최신 catalog를 채택하지만 초기 대화상자 revision은 그대로였으며, 모의 DOM에서 `[7, 7]` 요청과 화면 state 8을 재현했다. 서비스 충돌 차단은 정상이고 picker의 복구 경로를 추가했다.

`chooseCandidateLinks`는 충돌 후 저장을 잠그고 **선택 유지하고 최신 목록 검토**를 제공한다. 검토는 `platform.load()`만 호출한다. 자동 저장·자동 revision 대체·후보 재조회·권한 재요청은 하지 않는다. 사용자가 최신 대상과 변경된 선택을 확인한 뒤 별도 저장을 눌러야 한다.

- 최초 보관함 ID를 고정한다. 삭제된 보관함/그룹을 첫 항목이나 미분류로 몰래 대체하지 않는다.
- 최신 이름/경로를 표시하고, 이미 저장된 URL만 선택에서 제외하며 제외/유지 개수를 알린다. 새 항목은 자동 선택하지 않는다.
- `needsReview`를 실제 submit 함수에서도 검사한다. `setDialogBusy(false)`의 이전 disabled 복원 이후에도 잠금 상태를 다시 계산한다.
- 체크박스 변경이 충돌 안내를 지우지 않게 한다. 검토 응답은 대화상자 세대 및 revision을 검사하여 취소 후 도착하거나 더 오래된 응답을 무시한다.
- 검토 후 또 바뀌면 다시 충돌 안내로 돌아간다. 일반 저장 실패는 기존 선택을 보존하고 재시도할 수 있다.
- `test-support/candidate-picker-fixture.mjs`의 load seam과 `sidepanel-candidate-conflict.test.mjs` 38개 회귀로 읽기/저장 횟수, 선택·검색 유지, 삭제 대상, 중복 제거, 재충돌, 취소, 읽기 실패를 검증했다.
- `qa/persona-audit/capture.mjs` variant 9는 새 명시적 검토 흐름을 실행한다. 해결된 `conflict-reopen` 기록만 제거했다.

**수동 링크 입력·수정·단일 링크 이동도 0.1.31에서 완료됐다.** `linkDialog`가 세 모드를 처리하며 `moveDialog`는 move 모드 진입점이다. 원래 보관함 ID를 고정하고 최신 이름·주소·전체 경로를 비교한다. 변경된 기존 링크는 명시적 확인 체크 후 별도 저장, 삭제된 기존 링크는 재생성/대체하지 않는다. 수동 추가의 삭제된 보관함/그룹만 직접 재선택할 수 있다. 같은 이름의 상위 그룹 사이 이동도 전체 경로 ID로 판별한다. 공용 form의 `afterSubmit` 후처리가 busy 복원 이후에도 잠금을 유지한다. 당시 남겨둔 검색 해제/결과 펼침은 3번 과제로 0.1.37에서 완료했다.

**이름·색상도 0.1.32에서 완료됐다.** `nameDialog`와 `groupColorDialog`는 `metadataDialog`를 사용한다. 원래 대상 고정, 최신 이름·색상·전체 경로 비교, 명시적 확인 후 별도 저장, 삭제된 대상/상위 그룹과 최대 깊이 보호가 포함된다. 전역 작업인 새 보관함 추가는 원래 보관함이 없어져도 검토 후 진행할 수 있다. 색상 검증 오류 초점도 재활성화 후 HEX 입력으로 돌아온다. 공용 full-source fixture의 실제 색상 편집기를 사용한 68개 회귀로 검증했다.

**그룹·일괄 링크 이동도 0.1.33에서 완료됐다.** `moveGroupDialog`/`bulkMoveDialog`는 공용 `treeMoveDialog`를 사용한다. 원래 보관함·그룹·선택 링크 ID를 고정하고 최신 하위 내용/제목/주소/전체 경로와 목적지를 비교한다. 변경됐다면 확인 체크 후 별도로 이동하며, 삭제된 출발지를 다른 ID로 대체하거나 남은 일부만 이동하지 않는다. 삭제/자손 순환/최대 깊이로 무효가 된 목적지는 선택값을 보존한 채 차단하고 직접 재선택한다. 최신 이동 수와 같은 위치의 no-op를 확인한다. 목록 인덱스는 검토한 catalog마다 한 번 계산한다. 신규 40개 회귀로 읽기/쓰기 분리와 비동기·성능 경계를 검증했다.

**링크 제거·그룹 제거·보관함 초기화는 0.1.34에서 완료됐다.** `destructiveDialog`가 원래 보관함/대상 ID를 고정한다. 최신 링크 제목·주소·경로, 그룹 제거 후 보존 위치와 하위 내용, 초기화할 그룹·링크 수와 전체 내용을 읽어 표시한다. 충돌 후에는 변경 유무와 무관하게 명시적 확인 체크 후 별도로 실행해야 한다. 삭제된 원래 대상/보관함은 대체하지 않으며 긴 영향 목록은 native details로 펼쳐 확인한다. 신규 54개 회귀로 동작/비동기/취소/초점/최신 범위만 적용을 검증했다.

**JSON 복원·복원 전 안전 사본 복구는 0.1.35에서 완료됐다.** `restoreDialog`가 두 경로를 처리한다. JSON 파일 내용은 창을 연 뒤 고정하고 충돌 시 최신 현재 목록·교체될 안전 사본을 읽어 확인한다. 복원 전 목록 복구는 최초 실행부터 실제 안전 사본 미리보기와 확인 체크를 요구한다. 검토마다 체크를 해제하며 사본이 없거나 손상되면 현재 목록/다른 목록으로 대체하지 않는다.

- `platform.previewBackup()` / `FAVMOA_PREVIEW_BACKUP`이 신뢰된 sidepanel에 현재 revision·catalog와 실제 `restoreCatalog` 또는 null을 같은 서비스 큐에서 읽기 전용 반환한다. GET/CONFLICT는 기존 compact 응답을 유지한다. 데모 미리보기는 변경 알림도 보내지 않는다.
- 기존 안전 사본의 교체/소비는 revision을 올린다. 검토한 revision으로 별도 실행하고 재충돌은 다시 잠근다. 최초 JSON 확인창은 기존 명시적 전체 교체 계약을 유지하며, 선택적으로 최신 미리보기를 할 수 있다. 충돌 후에는 미리보기·체크를 건너뛸 수 없다.
- 신규 59개 회귀(서비스/플랫폼 23 + UI 기능 12 + 독립 비동기 경계 24)로 데이터 변경 없는 미리보기, 고정 파일, 최신 대상, 사본 교체/소비, null/손상, 늦은 응답, 취소, 재시도를 검증했다.

**기존 Moa 가져오기도 0.1.36에서 완료됐다.** `legacyImportDialog`가 마지막 `confirmDialog` 호출을 대체한다. 충돌 뒤 반복 실행을 막고 `platform.load()`로 최신 **대상 catalog**만 검토한다. 원본을 읽거나 쓰지 않는 검토 뒤 확인 체크와 별도 가져오기를 요구하며, 재검토/재충돌마다 체크를 해제한다. 초기 명시적 가져오기는 기존 동작을 유지한다.

- 원본 nfs 목록은 실제 가져오기 실행 때 읽는다. 대상 검토는 원본 내용·가져올 개수의 미리보기가 아니라는 문구를 표시한다. 원본 변경은 catalog revision과 별개이며 UI가 원본 스냅샷을 확인했다고 주장하지 않는다.
- 현재 catalog에 이전 기록이 남은 워크스페이스를 중복 복사하거나 자동 동기화하지 않는다. Undo/JSON 복원으로 이전 이력이 달라질 수 있어 영구적인 중복 방지라고 표현하지 않는다. 성공/취소/저장 실패에서 원본과 기존 보관함을 보존한다.
- 0개 가져오기 + warnings를 빈 목록으로 오안내하던 경로도 수정했다. 결과를 확인 필요로 표시하되 원본별 내부 경고 문자열을 그대로 출력하지 않는다. 신규 30개(실제 서비스 연결 기능 11 + 독립 비동기/실패 경계 19)로 검증했다.
- 이 완료는 큐에서 지정한 대화상자들의 복구 계약이며 모든 종류의 외부 충돌이나 실제 Chrome 사용 검증을 뜻하지 않는다. 서비스/모델/권한은 바꾸지 않았다.

### 3. 수동 추가·이동 결과 표시 — 0.1.37 완료

- 수동 추가·단일 링크 이동·그룹 메뉴 이동·드래그 콜백의 검색/접힘 숨김을 RED 재현했다. 모델 `addLink`/`moveLink`/`moveGroup`에 선택적 `revealTarget`을 추가해 내용 변경과 목적지 조상 펼침을 단일 저장·revision·Undo로 처리한다. 그룹 자체/하위의 접힘은 유지한다.
- `revealTreeResult`는 성공한 실제 결과를 찾아 검색과 화면상 접힘 억제를 해제한다. 대화상자 종료와 보류 렌더를 고려한 결과 초점·스크롤을 처리한다. 이후 다른 보관함/새 대화상자(열었다 취소한 경우 포함)/열린 테마의 초점을 빼앗지 않는다.
- 드래그 저장 전 보관함·대화상자 세대를 캡처해 응답 대기 중 사용자가 새 맥락을 선택한 경우 결과 표시만 생략한다. 완료된 이동은 보존한다. 동일 위치 이동·실패·취소·충돌 검토에서 불필요한 검색 해제/자동 펼침이 없다.
- 신규 58개(모델/서비스28 + UI 기능8 + 독립 경계22)로 한 번 저장·Undo·후속 명시 폴딩 보존·실패/취소/no-op·비동기 초점 보호를 검증했다. 드래그 콜백 시험은 실제 네이티브 포인터 검증을 뜻하지 않는다. 기존 일괄 담기/이동과 일반 링크 수정 계약은 유지한다.

### 4. 보관함 전환 시 검색 정리 — 0.1.38 완료

- 실제 선택기 핸들러의 검색 잔존을 RED 재현했다. 다른 유효 보관함을 선택하면 검색·임시 보기 상태를 비우고 다중 선택을 즉시 종료한다. 같은 보관함이나 없는/빈/삭제된 값은 현재 맥락과 정상 예약 작업을 보존한다.
- 보기 전환에 저장·revision·Undo 변경이 없다. 보류 렌더 중에도 이전 선택을 종료하고 보류된 결과 초점을 취소한다. 드래그 저장 전 보기 세대를 캡처해 A→B→A 왕복 뒤 늦은 결과 표시가 새 검색/초점을 덮지 않도록 한다. 이동 저장 자체는 보존한다.
- 신규16개(기본 정책·실제 서비스8 + 독립 비동기8)와 기존 회귀를 통과했다. 명시적 사용자 전환만 수정하고 프로그램 내부 보관함 변경·저장된 접힘·기존 충돌 복구 계약은 유지한다. 감사 variant5를 회귀 기대값으로 전환했고 기존 감사 원장은 그대로 보존한다.

### 5. 순서 독립 다중 키워드 검색 — 0.1.39 완료

- 메인 검색 입력으로 역순 단어와 제목/주소/조상 경로의 교차 단어가 숨겨지는 RED를 재현했다. 공백으로 나눈 모든 단어(AND)를 같은 링크의 제목·저장 URL·그룹 경로에서 찾는 범위로 확정했다. 문자 그대로 부분 검색이며 정규식/연산자/따옴표 구문/유사어/URL 디코딩은 추가하지 않았다.
- 렌더당 단어 정규화·중복 제거 후 `projectGroup`이 해당 그룹에서 남은 단어만 자기 분기로 전달한다. 다른 링크·형제·사촌 간 일치 상태를 공유하지 않는다. 그룹 경로가 모든 단어를 포함하면 하위 자료를 포함하며 원래 경로·개수·선택·접힘·현재 페이지 계약을 유지한다. 후보 선택창은 기존 문구 검색을 유지한다.
- 신규21개(기능/대량9 + 독립 경계12), 검증된 10,000링크·32단계 catalog와 원본 불변을 통과했다. 감사 variant1을 역순 결과 회귀 기대값으로 전환했고 원본 감사는 보존했다. 실제 브라우저 체감속도·스크린리더 검증은 아니다.

### 6. 대량 후보 선택 반복 줄이기 — 0.1.40 완료

- 405개 후보에서 보이는 범위만 선택되어 더 보기/선택을 반복하는 흐름을 재현했다. 같은 후보 조회 1회 상태에서 표시 행만 200→400→405로 늘리면 선택 가능 범위도 늘어, 조회 누락이나 1,000개 제한이 아닌 `shown` 범위임을 확인했다.
- 공용 탭·북마크 창에 ‘전체 N개 선택’ / ‘검색 결과 전체 N개 선택’을 추가했다. 표시 행보다 결과가 많을 때만 나타나고, 준비된 검색 결과 전체를 기존 선택에 더한다. 반복 클릭은 해제하지 않는다. 기존 보이는 항목 선택과 전체 선택 해제를 유지한다.
- 기존 숨은 선택과의 합집합이 1,000개를 넘으면 선택 전체를 그대로 두고 안내한다. 임의 첫 1,000개나 일부만 선택하지 않는다. 결과·표시 수/화면 밖 선택 수/범위 안내를 표시하며 버튼·표시 체크박스를 재생성하지 않아 초점 DOM을 유지한다.
- 선택 자체는 쓰지 않는다. 별도 담기까지 원본·저장 목록을 보존하고 로딩·저장·검토 중 또는 취소/대체된 이전 버튼은 새 선택을 바꾸지 않는다. 최신 검토에서 이미 저장된 후보를 제외한 뒤 범위를 갱신하며 기존 충돌 잠금과 단일 저장·Undo를 유지한다.
- 신규26개(기능/실제 서비스16 + 독립 비동기/원본 경계10) 통과. 합성 감사 대량 분기에 단축 선택·표시 행 유지·무저장·전체 해제·검색 보존 50대조를 더했다. **전체 선택의 반복은 줄였지만 후보 내용을 개별 검토하는 더 보기는 유지**하므로 기존 `large-list-pagination` 마찰 10건은 잔여 검토 부담으로 보존했다. 실제 브라우저 체감/접근성 검증은 아니다.

### 7. 가져오기 중 새 대상 그룹 — 0.1.41 완료

- 같은 가져오기 창에서 새 그룹을 지정하는 버튼 부재와 원자적 저장 명령 부재를 RED로 재현했다. ‘새 그룹에 담기’는 이름/위치 초안만 열고 마지막 ‘새 그룹에 N개 담기’에서 그룹과 선택 링크를 함께 만든다. 기존 모드 전환·검색 중 초안을 유지하며 전체 취소에는 빈 그룹이 남지 않는다.
- `addLinksToNewGroup`은 기존 링크 검증·중복 방지·1,000개 및 catalog 크기/노드 제한을 재사용한다. 루트는 명시적 null, 부모는 유효 ID를 요구하고 그룹 ID는 모델에서 생성한다. 이름 1~80자, 제어 문자·32단계 초과를 차단한다. 그룹·링크·자동 펼침이 한 저장/revision/Undo이며 실패·충돌에 부분 생성이 없다.
- 목적지는 전체 경로와 ID로 구별한다. 최신 검토 후 이름/검색/남은 선택을 유지하고 삭제·깊이 초과 부모는 무효 선택으로 남긴다. 다른 보관함을 고르면 같은 ID나 이전 최상위 선택도 자동 재사용하지 않고 위치를 직접 다시 고르게 한다. 늦은 검토와 이전 버튼은 새 창을 건드리지 않는다.
- 신규60개(모델23 + 서비스10 + UI 기능11 + 독립 UI 경계16), 전체1,186개와 합성300개/2,790대조 통과. 감사 담기 분기에 새 그룹 초안·무저장·검색/선택 유지·그룹 생성과 링크 단일 명령 검증을 추가했고 원본 감사는 보존했다.
- Ego Lite의 분리된 localhost:49141 예시 화면을 380×820에서 직접 조작했다. 빈 이름의 native validation·취소 무생성·새 그룹/링크 저장·생성 그룹 초점·한 번 Undo·기존 모드 전환 시 숨긴 필드가 저장을 막지 않음을 확인했다. 스크린샷3장을 저장·재열람했고 테스트 공간/서버를 종료했다. Chrome 설치 확장·실제 권한/개인 탭·북마크·스크린리더 전체 검증은 아니다.
- 등록 후보 1~7이 완료되어 추가 무의미한 변경을 만들지 않는다. 원격 전달 확인 뒤 예약을 중지하고, 별도 명시한 제약을 자동으로 범위 확대하지 않는다.

## 완료 기록

- **0.1.29 / 2026-09-25:** 1번 구현·검증 완료. 662개 테스트, 300개 합성 시나리오, 39파일 패키지 검사 통과. 정확히 5 MiB인 Undo의 폴딩 1바이트 초과 예외도 회귀 테스트로 보호한다. [QA 근거](QA-0.1.29.md).
- **0.1.30 / 2026-09-25:** 2번 중 탭·북마크 가져오기 충돌 복구 완료. 700개 테스트, 300개 합성 시나리오/2,650대조, 39파일 ZIP 검사 통과. 선택·검색어 보존, 명시 검토, 삭제 대상 재선택과 비동기 응답 경계 포함. 불편 기록 50 → 40건(실사용자 성과가 아님). [QA 근거](QA-0.1.30.md).
- **0.1.31 / 2026-09-25:** 2번 중 수동 링크 추가·수정·단일 이동 완료. 754개 테스트(신규54), 기존 합성 300개/2,650대조, 39파일 ZIP 검사 통과. 원래 보관함 고정·최신 내용 비교·명시 확인·동일이름 경로 변경 감지·삭제 대상 보호 포함. 불편 집계는 40건으로 동일하며 이번 범위는 별도 회귀54개로 검증한다. [QA 근거](QA-0.1.31.md).
- **0.1.32 / 2026-09-25:** 2번 중 이름·그룹색 완료. 822개 테스트(신규68), 기존 합성 300개/2,650대조, 39파일 ZIP 검사 통과. 삭제 대상 무대체·상위 경로/깊이 검토·색상 오류 초점 복구 포함. [QA 근거](QA-0.1.32.md).
- **0.1.33 / 2026-09-25:** 2번 중 그룹·일괄 링크 이동 완료. 862개 테스트(신규40), 기존 합성 300개/2,650대조, 39파일 ZIP 검사 통과. 최신 영향 범위와 목적지 비교·원래 선택 고정·부분 이동 차단·순환/깊이 재검토 및 대량 목적지 반복 계산 제거 포함. [QA 근거](QA-0.1.33.md).
- **0.1.34 / 2026-09-25:** 2번 중 링크 제거·그룹 제거·보관함 초기화 완료. 916개 테스트(신규54), 기존 합성 300개/2,650대조, 39파일 ZIP 검사 통과. 최신 영향·그룹 내용 보존 위치·원래 대상 고정·충돌마다 별도 확인 포함. [QA 근거](QA-0.1.34.md).
- **0.1.35 / 2026-09-25:** 2번 중 JSON 복원·복원 전 안전 사본 복구 완료. 975개 테스트(신규59), 기존 합성 300개/2,650대조, 39파일 ZIP 검사 통과. 읽기 전용 실제 미리보기·고정 파일·최신 현재 목록 및 사본 영향·검토 후 별도 확인/실행 포함. [QA 근거](QA-0.1.35.md).
- **0.1.36 / 2026-09-25:** 2번의 마지막 지정 대화상자(기존 Moa 가져오기) 완료. 1,005개 테스트(신규30), 기존 합성 300개/2,650대조, 39파일 ZIP 검사 통과. 대상만 읽는 검토·원본 조회 시점 명시·별도 확인/실행·0개+경고 오안내 수정 포함. 다음은 3번. [QA 근거](QA-0.1.36.md).
- **0.1.37 / 2026-09-25:** 3번 완료. 1,063개 테스트(신규58), 기존 합성 300개/2,650대조, 39파일 ZIP 검사 통과. 수동 추가·단일/그룹/드래그 이동의 목적지 표시·원자적 자동 펼침·Undo·no-op·비동기 초점 보호 포함. 다음은 4번. [QA 근거](QA-0.1.37.md).
- **0.1.38 / 2026-09-25:** 4번 완료. 1,079개 테스트(신규16), 합성 300개/2,670대조, 39파일 ZIP 검사 통과. 명시적 보관함 전환 검색 정리·선택 즉시 종료·같은/무효값 보존·왕복 전환 후 늦은 결과 보호 포함. 검색 잔존 10사례 해소로 합성 마찰 기록40→30건이며 실사용자 개선률은 아니다. 우선순위1~4 구현·검증을 마쳤고 다음은 5번. [QA 근거](QA-0.1.38.md).
- **0.1.39 / 2026-09-25:** 5번 메인 트리 완료. 1,100개 테스트(신규21), 합성300개/2,690대조, 39파일 ZIP 검사 통과. 순서 독립 AND·제목/URL/조상 경로·분기 격리·선택/접힘 보존·1만링크/32단계 확인 포함. 역순 검색10사례 해소로 합성 마찰30→20건이며 실사용자 개선률은 아니다. 다음은 6번. [QA 근거](QA-0.1.39.md).
- **0.1.40 / 2026-09-25:** 6번 완료. 1,126개 테스트(신규26), 합성300개/2,740대조, 39파일 ZIP 검사 통과. 결과 전체 단축 선택·1,000개 합집합 제한·숨은 선택·비동기/충돌 보호·실제 서비스 단일 저장/Undo 포함. 개별 검토의 더 보기는 유지하므로 합성 마찰20건을 보존한다. 다음은 7번. [QA 근거](QA-0.1.40.md).
- **0.1.41 / 2026-09-25:** 7번 및 등록 후보 전체 완료. 1,186개 테스트(신규60), 합성300개/2,790대조, 39파일 ZIP 검사 통과. 가져오기 중 새 그룹 초안·단일 저장/Undo·취소 무생성·부모/보관함 충돌 복구와 별도 localhost UI 직접 검증 포함. 합성 마찰20건은 한 단계 Undo와 개별 검토 페이지네이션의 남은 제약이다. [QA 근거](QA-0.1.41.md).
- 각 변경 단위를 커밋·푸시하고 원격 HEAD를 확인한다. 후속 작업을 재개한다면 Git 이력과 원격을 먼저 확인하고 미전달 변경이 있으면 전달을 마친 후 이어간다.
- 후속 실제 브라우저 점검에서 일부 자동 클릭/Enter는 반영되지 않았지만 접근성 Expand는 반영됐다. 콘솔 오류는 없으며 입력 도구와 제품 원인을 분리하지 못했다. 포인터/키보드 전체 검증으로 주장하지 말고 다음 브라우저 점검에서 먼저 분리 확인한다. 기존 단위 테스트의 포인터 보호 회귀는 통과했다.
- 0.1.30의 별도 localhost:4130 UI 검증은 내부 브라우저 webview 연결 실패로 미실행이다. 두 연결 시도 후 탭 없음 확인, 임시 서버 종료. 다음 실행에서는 브라우저 가용성을 확인하되 보안 정책으로 차단된 Chrome 관리 URL을 우회하지 않는다. 기존 설치 경로에 패키지 파일만 적용했으며 Chrome 확장 새로고침은 사용자 확인이 필요하다.
- 0.1.31도 localhost:4131에서 같은 webview 연결 실패·빈 탭 목록을 확인했다. 실제 화면 검증은 미실행이며 서버 종료. 기존 설치 경로의 프로그램 파일만 백업 후 교체했고 소스/ZIP/설치 39파일 일치를 확인했다. 반복 연결 시도로 회차를 소모하거나 실제 브라우저 검증 완료로 주장하지 않는다.
- 0.1.32는 최근 두 회차 연결 실패를 반복하지 않았으며 네이티브 화면 점검은 미실행이다. 소스/ZIP/기존 설치 경로 39파일 일치와 로컬 백업을 확인했지만 Chrome 런타임 반영은 사용자 새로고침 확인이 필요하다.
- 0.1.33도 네이티브 화면 점검은 미실행이다. 합성 DOM 점검·독립 검토 및 소스/ZIP/기존 설치 39파일 일치를 확인했다. Chrome 확장 새로고침은 사용자 확인이 필요하다.
- 0.1.34도 실제 Chrome 화면 점검은 미실행이다. 기존 설치 경로를 백업 후 갱신하고 소스/ZIP/설치 39파일 일치를 확인했다. 사용자 저장 데이터·탭·북마크는 테스트에 사용하지 않았으며 확장 카드 새로고침은 사용자 확인이 필요하다.
- 0.1.35도 실제 Chrome 화면 점검은 미실행이다. 동일 설치 경로의 프로그램 파일만 백업 후 갱신하고 소스/ZIP/설치 39파일 일치를 확인했다. Chrome 런타임 반영은 사용자 확장 카드 새로고침 확인이 필요하다.
- 0.1.36도 실제 Chrome 화면 점검은 미실행이다. 같은 설치 경로를 백업 후 프로그램 파일만 갱신하고 소스/ZIP/설치 39파일 일치를 확인했다. 원본 개인 데이터·탭·북마크는 테스트에 사용하지 않았다. Chrome 확장 카드 새로고침은 사용자 확인이 필요하다.
- 0.1.37도 실제 Chrome 화면 점검은 미실행이다. 합성 DOM·실제 서비스/모델 검증과 독립 경계 검토를 수행하고 같은 설치 경로의 프로그램 파일만 백업 후 갱신했다. 소스/ZIP/설치 39파일 일치, 개인 데이터 미사용을 확인했다. Chrome 확장 카드 새로고침은 사용자 확인이 필요하다.
- 0.1.38도 실제 Chrome 화면 점검은 미실행이다. 기존 설치 경로를 백업 후 프로그램 파일만 갱신했고 소스/ZIP/설치39파일 일치를 확인했다. 개인 데이터·실제 탭·북마크를 사용하지 않았다. Chrome 확장 카드 새로고침은 사용자 확인이 필요하다.
- 0.1.39도 실제 Chrome 화면 점검은 미실행이다. 검색 안내의 HTML 연결·합성 UI와 계산은 확인했지만 실제 화면/입력/체감속도/스크린리더 증거로 확대하지 않는다. 같은 설치 경로를 백업 후 갱신했고 소스/ZIP/설치39파일 일치를 확인했다. Chrome 확장 카드 새로고침은 사용자 확인이 필요하다.
- 0.1.40도 실제 Chrome 화면 점검은 미실행이다. 합성 후보·모의 DOM·실제 저장 서비스 경계로만 검증했다. 같은 설치 경로를 백업 후 프로그램 파일만 갱신했고 소스/ZIP/설치39파일 일치를 확인했다. Chrome 확장 카드 새로고침은 사용자 확인이 필요하다.
- 0.1.41은 localhost 예시 UI를 Ego Lite에서 직접 확인했으나 설치 Chrome 확장 런타임과는 구분한다. 기존 설치 경로를 백업 후 프로그램 파일만 갱신하고 소스/ZIP/설치39파일 일치를 확인했다. 사용자 저장 데이터·실제 탭·북마크는 사용하지 않았으며 Chrome 확장 카드 새로고침은 사용자 확인이 필요하다.
