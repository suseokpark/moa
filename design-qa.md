# FAVMOA 0.1.15 디자인 QA

검증일: 2026-09-23 · 대상: 사이드 패널과 설치 도움말

final result: passed

판정 범위는 이번 디자인 변경의 로컬 시각·상호작용 QA다. 실계정 연동·실제 설치 확장의 인수 테스트를 통과했다는 뜻이 아니다. 확인한 범위에 미해결 P0/P1/P2는 없다. P3와 미검증 항목은 아래에 별도로 남긴다.

## 목표와 환경

- 사용자가 요청한 방향: 더 산뜻한 화면, 최신 공식 UI 사례 조사, 범용 템플릿 같은 장식은 피하기.
- 기준: 기존 FAVMOA 브랜드·그룹 → 섹션 → 링크 구조·로컬 저장/백업 정책. 다른 제품을 복제하거나 새 랜딩 페이지를 만드는 작업이 아니다.
- 연구와 적용 판단: [UI 리프레시](docs/UI-REFRESH-0.1.15.md).
- 구현 미리보기: `http://127.0.0.1:4175/extension/notion-favorite-sections/sidepanel/sidepanel.html`.
- Chrome localhost + 합성 데이터. 실제 Chrome 확장 저장소, 사용자 북마크, 실계정 Notion 데이터와 분리했다.
- 360×850 및 320×740의 패널 폭, 밝은/어두운 테마, reduced-motion을 확인했다. 도움말도 320px 폭에서 검사했다.

## 스크린샷 비교 근거

모든 스크린샷은 현재 실행에서 원본 바이트를 파일에 저장한 뒤 같은 파일을 다시 열어 확인했다. 리사이즈·합성·후보정하지 않았다.

| 파일 — 모두 artifacts/design-0.1.15/ 아래 | 조건 / 용도 |
|---|---|
| 01-before-tree.jpg | 0.1.14, 360×850 viewport, 360×1000 full-page 이미지. 합성 링크 2개, 그룹·섹션 열림 |
| 02-after-tree.jpg | 0.1.15, 같은 viewport·데이터·접힘 상태, 360×985 full-page 이미지 |
| 03-after-dialog.jpg | 360px에서 URL과 선택적 이름 입력, 저장 위치는 접힘 |
| 04-dark-search.jpg | 수정 전 증거. 기본 검색 지우기와 앱 지우기가 중복됨. 최종 합격 화면으로 사용하지 않음 |
| 05-dark-narrow-menu.jpg | 수정 전 증거. 하단 메뉴가 viewport 아래로 잘림 |
| 06-narrow-menu-fixed.jpg | 320×740, 다크. 메뉴가 위로 열려 6개 조작이 화면 안에 표시됨 |
| 07-light-narrow-dialog.jpg | 320×740, 밝은 테마. 폼과 저장 위치 키보드 초점 |
| 08-light-narrow-settings.jpg | 320×740. 백업·가져오기·초기화의 순서와 간격 |
| 09-help-narrow.jpg | 처음 연 도움말의 기본 넓은 viewport 확인용. 이름과 달리 좁은 화면 증거는 아님 |
| 10-help-320px.jpg | 실제 320×740 도움말 검증, body.scrollWidth=320 |
| 11-dark-search-fixed.jpg | 320×740. 지우기 중복 해결, 긴 한글 검색 결과 및 초점 |
| 12-final-tree.jpg | 최종 CSS 소스 다시 로드 후 320px 패널. 추가·수정 후 합성 링크 3개. 동일 데이터 비교에는 사용하지 않음 |

01/02를 **한 번의 이미지 비교 입력에 함께 제공**해 검사했다. 두 별도 화면을 기억으로 비교하거나, 합성 링크 개수가 다른 마지막 화면을 전후 비교로 간주하지 않았다.

## 다섯 시각 영역 판정

| 영역 | 의도적 변경과 보존 | 결과 |
|---|---|---|
| Typography | 링크·그룹 14px, 조작·섹션 12px, 주소·개수 11px. 시스템 글꼴 유지. 더 진한 보조 글자, 긴 제목 말줄임 | 통과. 한글/영문 혼합 제목과 기준선에 관찰된 잘림 없음 |
| Spacing/layout | 상단 저장 안내의 카드를 한 줄로 축소. 트리 들여쓰기·2단계 접힘 유지. 제목 행 38px, 조작 최소 32px | 통과. 320px body 가로 넘침 없음. 메뉴 하단 잘림은 위 배치/내부 스크롤로 수정 |
| Colors | 흰 불투명 바탕, 옅은 중성 보조 면, 절제된 녹색 포인트. 현재 링크는 틴트·왼쪽 선·텍스트를 함께 사용 | 통과. 색만으로 상태를 표현하지 않음. 다크에서도 입력/본문/초점 구분 |
| Assets | 기존 로고·사용자 이모지 유지. Phosphor Regular 8개를 로컬 SVG로 사용, MIT 포함. 새 사진/일러스트/외부 폰트 없음 | 통과. 검색·폴딩·메뉴·링크 아이콘 일관성 및 실제 렌더링 확인 |
| Copy/content | “링크를 모으고, 바로 찾기”, “링크 추가” 등 짧고 명확한 조작. 자세한 저장 범위는 사용 안내로 이동 | 통과. 그룹/섹션/링크 명칭 유지. 예시 데이터 표식·클라우드 미제공·백업 주의는 숨기지 않음 |

## 수정 루프

1. **기준 화면 → 첫 스타일 적용:** 같은 합성 목록으로 01/02 비교. 여러 박스가 경쟁하던 강조를 줄이고 글자 대비·아이콘 정렬을 개선했다. 이것은 의도적 변경이며 픽셀 복제의 오차가 아니다.
2. **검색 상태:** 다크 검색에서 지우기 동작이 두 번 보임을 발견(04). WebKit 기본 취소 버튼만 감추고 이름이 있는 앱 버튼을 유지. 11에서 중복이 없음을 확인했다.
3. **작은 높이의 메뉴:** 아래쪽 링크 메뉴가 잘리는 P2를 발견(05). 화면 공간을 계산해 위/아래 배치와 최대 높이를 정하도록 수정. 06에서 top=314.5, bottom=542.5, viewport 높이=740을 확인했다. 메뉴 높이를 제한하는 세 가지 단위 테스트를 추가했다.
4. **최소 Chrome 버전 호환:** 별도 소스 리뷰에서 mask 접두사 누락 P2 발견. Chrome 116~119용 `-webkit-mask`를 추가하고 테스트 실패 → 통과를 확인했다. 이 구형 버전 자체를 실행한 것은 아니다.
5. **최종 재확인:** 검색 결과 없음/전체 복귀, 현재 위치 재탐색, 도움말 반응형, 최신 CSS 재로드 및 아이콘 확인. 328개 자동 테스트, 8개 기존 Notion 브라우저 회귀 시나리오와 패키지 검증 완료.

## 상호작용과 접근성 근거

- URL + 이름으로 합성 링크 저장, 전체 2 → 3개 확인. 긴 한글 이름으로 수정 후 가로 넘침 없이 말줄임을 확인했다.
- 그룹을 접고 검색하면 내부 링크를 찾을 수 있음. 1/3 및 0/3 결과, 검색 지우고 전체 보기, Escape 지우기를 확인했다.
- 현재 링크가 있는 그룹을 접은 뒤 “저장 위치 보기” → 그룹/섹션 펼침, 현재 링크로 초점 이동 확인.
- 320px 입력 대화상자 폭 296px. Tab 이동과 취소 후 “링크 추가” 버튼으로 초점 복귀 확인. 취소한 URL은 저장되지 않았다.
- 메뉴 Escape 종료 및 메뉴 시작점으로 초점 복귀. 기본 열림 상태의 6개 메뉴 조작 확인.
- 상단 “백업” → 설정 펼침 및 백업 영역 초점 이동. 불러오기와 복원 전 안전 사본, 맨 아래 초기화가 구분되어 보인다. 파괴적 동작은 실행하지 않았다.
- 밝은/어두운 불투명 배경별 텍스트 토큰은 4.5:1 이상, 실제 입력 경계/초점은 3:1 이상으로 계산 검사. 밝은 보조 텍스트 최소 4.93:1, 어두운 보조 텍스트 최소 5.96:1.
- 아이콘은 aria-hidden, 아이콘 전용 버튼은 접근 가능한 이름 유지. `aria-current`, 현재/열림 텍스트, 펼침 상태를 소스·합성 DOM 검사로 보호한다.
- reduced-motion emulation에서 버튼 transition=0s. forced-colors 스타일은 소스 검사만 했으며 실제 OS 모드는 미검증.
- 미리보기에서 조회된 warn/error 콘솔 로그 없음. 이 관찰은 모든 가능한 실행 환경에서 오류가 없다는 보증은 아니다.

## 테스트와 패키지

- `npm test`: **328/328 통과**, 0 실패/건너뜀. 새 `sidepanel-design.test.mjs`의 12개 회귀 검사 포함.
- `test/browser-suite.html`: **8/8 통과**. 직접 저장/복구, 구형 프로필, 저장 실패 보존, 키보드/폼, 좁은 화면/긴 40개 목록/확대/다크, 복합 아이콘, 현재 페이지/하위 트리, 제목 캐시.
- 이 8개는 기존 Notion 합성 화면의 회귀 검사다. 새 범용 패널의 실제 열린 탭/실제 Chrome 북마크 동작 증거로 대체하지 않는다.
- `npm run package:extension`: 37개 런타임 파일, **447168 bytes**.
- ZIP: `artifacts/favmoa-v0.1.15.zip`; 압축 해제: `artifacts/favmoa-v0.1.15/`.
- `--verify`로 현재 소스와 바이트 단위 일치 확인, `unzip -t` 37개 모두 통과. 압축 해제 폴더의 37개 파일도 각각 현재 소스와 바이트 단위 일치.
- SHA-256: `2208c2966b430affdca1555e74fdb396396a21793d83dcab00c04d7297234b17`.
- 공개 확장 ID: `ebbgmhdbonpillbfjapebljagnbjembj`. 공개키·데이터 저장 키·권한 범위 유지. Google 권한과 클라우드 런타임은 ZIP에서 제외된 상태 유지.
- 외부 원본이나 GitHub에 쓰지 않았고, commit/push/웹스토어 배포하지 않았다.

## 남은 한계와 P3

- 실제 설치 확장 URL은 브라우저 접근 정책으로 조작할 수 없었다. 우회하지 않았다. 업데이트된 설치 확장과 실제 Notion 계정에서의 최종 사용자 확인은 별도다.
- 브라우저 파일 업로드의 권한 제한으로 JSON 파일 저장 → 불러오기 왕복은 미검증. 소스/단위 테스트와 UI 발견성만 확인했다. 제한을 우회하지 않았다.
- 새 패널의 화면 읽기 프로그램, 실제 고대비 OS, 실제 Chrome 116~119, 200% 브라우저 확대는 미검증. 기존 Notion fixture의 확대 검사는 별개다. 전체 WCAG 준수 선언이 아니다.
- P3: 메뉴를 열어 둔 채 창 높이를 바꾸면 즉시 재배치되지는 않는다. 닫고 다시 열면 계산된다. 이번에 검증한 각 고정 viewport에서 메뉴 잘림은 해결했다.
- Google 로그인/Drive 동기화는 계속 보류이며 이번 작업의 범위가 아니다.
